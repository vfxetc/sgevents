type: qube_callback
name: sgevents qube dev

callback: sgevents.dispatcher.qube:_test

filter:
    user.HumanUser.login: mboers
    project:
        type: Project
        id: 66

envvars:
    # Force devmode
    VEE_EXEC_ARGS: "--dev"
